{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd":"${workspaceFolder}\\duckdb-editing-points-filesystem\\",
            "command": "npm start -- --port 8080 --self-signed-cert",
            "name": "Launch CDF with SSL",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}